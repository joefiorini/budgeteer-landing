import React from 'react';

export default ({ theme }) => (
  <section className={theme.container}>
    <h3>Are you ready to reclaim the time and money to do more of what you love?</h3>
    <button>Sign Me Up</button>
  </section>
);
